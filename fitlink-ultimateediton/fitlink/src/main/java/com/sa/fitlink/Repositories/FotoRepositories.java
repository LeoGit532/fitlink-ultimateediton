package com.sa.fitlink.Repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import com.sa.fitlink.Entities.Foto;

public interface FotoRepositories extends JpaRepository<Foto, Long> {
    
}
